<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.dao.Study_roomMapper">

    <select id="queryRoomByNameLike" resultType="com.demo.entities.Study_room">
        select * from study_room where name like #{name}
    </select>
</mapper>